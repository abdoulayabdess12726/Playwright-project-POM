/**
 * Données de test — Utilisateurs SauceDemo
 *
 * Centraliser les données ici permet de les modifier en un seul endroit
 * si les credentials changent, sans toucher aux tests.
 */
export const USERS = {
  standard: {
    username: "standard_user",
    password: "secret_sauce",
  },
} as const;
